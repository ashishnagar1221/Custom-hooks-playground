const Home = () => {
  return (
    <div>
      <h1 className="text-2xl font-bold mb-2">Welcome — Hooks Playground</h1>
      <p className="text-gray-600">
        Use the navbar to open each hook demo. Each page logs to the console
        when mounted.
      </p>
    </div>
  );
};

export default Home;
